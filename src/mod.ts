export { IOperation, OT } from './control';
import {
  Delete,
  Insert,
  Operation,
  OperationType,
  exclusionTransform,
  inclusionTransform,
} from './charwise';

export const charwise = {
  Delete,
  Insert,
  Operation,
  OperationType,
  exclusionTransform,
  inclusionTransform,
};
