export { IOperation, OT } from './control';
import {
  exclusionTransform,
  inclusionTransform,
  Operation,
  OperationType,
} from './charwise';

export const charwise = {
  exclusionTransform,
  inclusionTransform,
  Operation,
  OperationType,
};
